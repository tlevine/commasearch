'''
import itertools

def index(db, url:str):
    indices = set()
    colnames = '???'
    for x in range(len(colnames)):
        for i in itertools.combinations(colnames, x):
            if a subset is not already an index
                'SELECT ((SELECT count(distinct column, column, column) FROM THE_TABLE) - (SELECT count(*) FROM THE_TABLE))'
                if that is zero, it's unique!
    raise NotImplementedError
'''

def index(db, url):
    raise NotImplementedError

def search(db, url:str):
    raise NotImplementedError
